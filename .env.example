# Required: base64-encoded 32-byte AES-256 key for cookie encryption
# Generate with: openssl rand -base64 32
COOKIE_ENCRYPTION_KEY=

# Optional: Postgres password (default: devpassword)
# POSTGRES_PASSWORD=devpassword
